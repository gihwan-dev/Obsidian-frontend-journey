---

## 📝 Daily Diary

### 🧭 의사결정

#### exem-ui (Table 컴포넌트)
- 컬럼 리사이즈 정책을 `우측 캐스케이드 + Flex Basis` 단방향 모델로 고정하고, 왼쪽 컬럼 불변 원칙을 유지하기로 결정했다. 복잡한 예외 처리보다 예측 가능성과 디버깅 용이성을 우선했다.
- 리사이즈 검증은 테스트 파일을 새로 분산하기보다 기존 테스트를 확장하는 방식으로 정했다. 리뷰 맥락을 한 곳에 모아 변경 추적 비용을 낮추기 위한 선택이었다.

#### claude-setup
- Claude 전용 스킬 자산을 Codex에서도 재사용하기 위해 `.claude` 기반 자산을 `.agents` 구조와 함께 관리하는 방향을 채택했다. 도구 간 호환성과 설치 자동화 재사용성이 기준이었다.

#### MFA (AlertLog 스펙)
- RTM AlertLog 문서는 신규 포맷보다 팀의 기존 마크다운 형식을 유지하기로 결정했다. 검토 속도와 커뮤니케이션 일관성을 우선했다.

### 🖥️ 개발 활동 (실행)

#### exem-ui
- 테이블 컬럼 리사이즈 로직을 Feature 레이어 중심으로 정리하고 정책 검증 테스트를 보강했으며, 이 작업은 `6bcbd93`, `2a4b29a`, `798f564`, `25fc075`를 포함한 당일 16개 커밋으로 누적됐다.

#### claude-setup
- "Codex에서도 기존 스킬을 호환 사용하고 싶다"는 요청을 기준으로 구조/설치 흐름을 정리해 `4536ff2` 커밋(`Codex/Claude 스킬 호환 및 설치 스크립트 추가`)으로 마무리했다.

#### React-Dashboard
- `xxs` 브레이크포인트 불일치 에러와 AlertLog 모달 표현 문제를 분리 대응해 `4d01a57`, `d4e9794`, `ce342c3` 커밋 단위로 반영했다.

#### Obsidian-frontend-journey
- "채팅 활동이 일기에 빠진다"는 문제를 기준으로 Codex 세션 경로를 재점검했고, `history.jsonl` 외 `sessions/archived_sessions`까지 추적하도록 정리한 뒤 일기 재반영까지 완료했다.

### 📈 결과/임팩트
- Table 리사이즈 정책이 코드/테스트/문서에서 같은 모델로 정렬되어 회귀 원인 추적이 쉬워졌다.
- 대시보드 브레이크포인트 오류와 AlertLog UI 이슈가 커밋 단위로 분리 정리되어 롤백/검증 경계가 명확해졌다.
- 볼트 변경이 당일 백업 커밋 21건, 마크다운 변경 45건으로 남아 작업 근거와 회고 재사용성이 높아졌다.

### 🧾 이력서 후보 문장
- 테이블 컬럼 리사이즈를 단방향 정책으로 재정의하고 테스트를 동반 보강해, 복잡한 UI 상호작용의 예측 가능성과 유지보수성을 동시에 개선했다.
- 다중 프로젝트(스킬 인프라, 대시보드, 문서화) 작업을 커밋 경계로 분리 운영해, 기능 안정화와 협업 전달 속도를 함께 끌어올렸다.

### 📓 기록 활동
- 오늘 수정한 노트 45건 중 주요 문서: [[클로드 사용량 분석]], [[AlertLog 데이터 가공 스펙]], [[AlertLog 화면 스펙]], [[AlortLog Detail 화면 스펙]], [[테이블 컬럼 리사이즈 기능 스펙]], [[Table 컬럼 리사이즈 모델 전환 (우측 캐스케이드 + Flex Basis)]], [[차세대 WebEnv 저장 구조]], [[위젯 빌더 리팩토링]], [[2026-02-11]], [[2026-02-12]]

### 💭 회고
하루 단위로 의사결정 기준과 커밋 결과를 함께 남기면 다음 작업에서 재설명 비용이 크게 줄어든다. UI 정책 변경은 테스트·문서·커밋 분리를 동시에 진행해야 품질과 속도를 같이 가져갈 수 있다.
