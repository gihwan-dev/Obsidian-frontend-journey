---

## 📝 Daily Diary

### 🧭 의사결정

#### exem-ui
- Phase 6 리사이즈 최적화는 최소 변경안으로 진행하고, `useLegendTable`의 `forceRender` 조건을 명시적으로 제한하는 방향을 선택(렌더 폭증 리스크를 먼저 차단하기 위해).

#### scheduling-automation
- 다과목 교사 과목별 시수 회귀는 기능 복구를 최우선으로 두고, 추가 최적화는 후속으로 분리(현장 요청 대응 속도 우선).

### 🖥️ 개발 활동 (실행)

#### exem-ui
- Phase 5(데이터 flush/Accessor 안전성) 구현 요청을 반영해 데이터 flush 파이프라인과 accessor 안정성을 확정하고 관련 리팩터링을 수행함.
- Phase 6/7 성능 병목을 해소하기 위해 렌더 경계 slice 구독 분리, 리사이즈 경로 렌더 동기화 최적화, pagination 브리지/상태 구독 로직 정리를 진행함.
- 의도(Phase 6·7 완료 상태 반영)와 결과(문서 커밋: `docs(root): [#0] 마일스톤 Phase 6·7 완료 상태 반영`)를 한 문장으로 정리해 변경을 기록함.

#### scheduling-automation
- 다과목 교사 과목별 시수 설정 회귀 복구 요청을 구현으로 반영하고, 결과 커밋(`fix(setup): [#0] 다과목 교사 과목별 시수 회귀 복구`)으로 마무리함.
- 센트리 초기 설정 도입 요청을 처리하고, 결과 커밋(`feat(observability): 센트리 초기 설정 적용`)을 남김.

### 📈 결과/임팩트
- 테이블 렌더 경계와 리사이즈 경로 최적화로 고빈도 리사이즈 상황의 렌더 폭증 리스크를 낮춤.
- 다과목 교사 시수 설정 회귀를 복구해 현장 운영 중단 리스크를 해소함.

### 🧾 이력서 후보 문장
- 테이블 리사이즈 고빈도 이벤트 병목을 제거하기 위해 렌더 경계/상태 구독을 재설계하고, 성능 최적화 커밋까지 일괄 반영했다.
- 현장 회귀 버그를 빠르게 복구하고 관측 도구(Sentry) 도입까지 이어지도록 릴리즈 안정성을 강화했다.

### 📓 기록 활동
- 새로 작성/수정한 노트: [[legend-state 도입]], [[성능 최적화 - Legent State 도입, Vanilla JS 기반 구조]], [[레거시WebEnv 저장 구조]], [[차세대 WebEnv 저장 구조]]

### 💭 회고
성능 이슈는 최소 변경으로 원인을 좁히고, 빠른 복구와 장기 최적화를 분리하면 속도와 안정성을 함께 확보할 수 있다.
