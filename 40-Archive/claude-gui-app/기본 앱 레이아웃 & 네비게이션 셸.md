# 기본 앱 레이아웃 & 네비게이션 셸

#claude-gui-app #즉시 #UI

## 요약
앱의 기본 레이아웃 구조(사이드바, 메인 영역, 상태바)와 페이지 간 네비게이션을 구성한다.

## 왜?
- 모든 기능 컴포넌트가 배치될 공간이 필요
- 초기에 레이아웃을 확정해야 이후 기능별 개발 시 충돌 없이 독립적으로 진행 가능
- 사이드바 기반 네비게이션은 다중 세션, 워크플로우 등 복잡한 기능을 수용할 수 있는 구조

## 어떻게?
1. 레이아웃 구조 설계:
   - 좌측 사이드바: 세션 목록, 프로젝트 탐색기, 설정 진입점
   - 중앙 메인 영역: 터미널, 워크플로우 에디터, 모니터링 등 주요 콘텐츠
   - 하단 상태바: 현재 세션 정보, 토큰 사용량, 연결 상태
2. React Router 또는 자체 라우팅으로 페이지 전환
3. 사이드바 ↔ 메인 영역 크기 조절 (resizable pane)
4. Tailwind CSS 또는 CSS Module로 스타일링 기반 구축

## 예상 효과
- 이후 모든 기능 개발이 일관된 레이아웃 안에서 진행
- 사용자가 앱의 전체 구조를 직관적으로 파악 가능
- 다중 패널 구성으로 동시 정보 확인 가능

## 작업 로그

### 2026-02-08

**구현 내용:**
- Zustand ui-store에 `AppView` 타입(`'welcome' | 'sessions' | 'projects'`)과 `activeView` 상태, `setActiveView` 액션 추가
- WelcomeView, SessionsView, ProjectsView 뷰 컴포넌트 생성 (placeholder)
- LeftSideBarContent 컴포넌트 생성 — Sessions/Projects 네비게이션 + Settings 버튼
- MainWindowContent에 activeView 기반 조건부 뷰 렌더링 로직 추가
- StatusBar 컴포넌트 생성 (세션 정보, 토큰, 연결 상태 placeholder)
- MainWindow에 StatusBar 통합 (ResizablePanelGroup 하단)
- 커맨드 팔레트용 뷰 전환 커맨드 3개 추가 (view-welcome, view-sessions, view-projects)
- i18n 키 추가 (en/ar/fr — 사이드바, 뷰, 상태바, 네비게이션 커맨드)

**생성 파일:**
- `src/components/views/WelcomeView.tsx` — 기본 Welcome 뷰
- `src/components/views/SessionsView.tsx` — Sessions placeholder 뷰
- `src/components/views/ProjectsView.tsx` — Projects placeholder 뷰
- `src/components/layout/LeftSideBarContent.tsx` — 사이드바 네비게이션 콘텐츠
- `src/components/layout/StatusBar.tsx` — 하단 상태바

**수정 파일:**
- `src/store/ui-store.ts` — AppView 타입 및 activeView 상태 추가
- `src/components/layout/LeftSideBar.tsx` — LeftSideBarContent 기본 렌더링
- `src/components/layout/MainWindowContent.tsx` — 뷰 전환 로직
- `src/components/layout/MainWindow.tsx` — StatusBar 배치
- `src/components/layout/index.ts` — StatusBar export 추가
- `src/lib/commands/navigation-commands.ts` — 뷰 전환 커맨드 3개 추가
- `locales/en.json`, `locales/ar.json`, `locales/fr.json` — i18n 키 추가
- `src/App.test.tsx` — WelcomeView 텍스트로 테스트 업데이트

**기술 결정:**
- shadcn Sidebar 대신 커스텀 사이드바 콘텐츠 사용 (shadcn Sidebar의 자체 Context가 Zustand와 충돌)
- URL 라우팅 대신 Zustand `activeView` 상태로 뷰 전환 (데스크톱 앱에 URL 불필요)
- 상태바는 24px placeholder로 구현 (실제 데이터 연결은 Phase 2에서)

**검증:**
- `pnpm check:all` 전체 통과 (typecheck, lint, ast-grep, format, rust checks, 32/32 tests)
