---

## 📝 Daily Diary

### 🧭 의사결정

#### exem-ui
- `@packages/react/src/table/spec/ColumnOrdering/WithColumnResize` 스토리를 기준 시나리오로 고정해 리사이즈/스크롤 동기화 버그를 검증하기로 결정했다. 임의 재현 대신 기준 스토리를 택해 회귀 범위를 명확히 하고, 하단/우측 고정 행 조합이라는 제약 조건을 먼저 해결 대상으로 잡았다.
- `useLayoutEffect` 타이밍에만 의존하는 ref 등록은 초기 `null` 이후 갱신 누락 위험이 있어 배제하고, ref 할당 자체를 테스트로 보장하는 쪽을 우선했다.

#### Obsidian-frontend-journey
- GitHub push 실패 원인을 100MB 초과 `.mov` 파일로 판단하고, 이력 보존보다 원격 푸시 복구를 우선해 대용량 추적 파일 제거/추적 중단 전략으로 정리했다.

### 🖥️ 개발 활동 (실행)

#### exem-ui
- Table 9분할 레이아웃의 스크롤 위임 구조를 재검토하고, top/bottom 가로 스크롤 및 우측 고정 행 세로 스크롤 동기화 경로를 점검했다.
- 사용자 요청(리사이즈 동작 버그 수정)을 `WithColumnResize` 스토리 기준으로 재현해 수정했고, `fix(react): [#82] Table > 버그 수정` 커밋으로 마무리했다.
- `RowPinning.browser.test.tsx` 중심으로 ref/scroll 전달 검증 케이스를 보강해 회귀 가능성을 줄였다.

#### Obsidian-frontend-journey
- 대용량 파일로 인한 push 실패를 해결하기 위해 불필요 파일 정리와 `.obsidian/workspace.json` 추적 중단을 적용했다.
- 당일 볼트 백업 자동 커밋 10건을 포함해 총 13개 커밋으로 문서/환경 상태를 안정적으로 동기화했다.

#### React-Dashboard
- Alert Log 모듈을 슬라이스 생성부터 Split View 모달, 3단 레이아웃, 오버레이 UX 개선까지 순차 구현했다.
- 홈 화면 dot-grid 배경 및 위젯 정렬 유지 개선을 완료해 시각 일관성과 레이아웃 안정성을 높였다.

### 📈 결과/임팩트
- Table 핵심 버그 수정 커밋(`[#82]`)으로 고정 행 + 리사이즈 조합에서의 스크롤 동기화 결함 재현 경로를 차단했다.
- React-Dashboard는 alert-log 관련 기능 커밋 5건과 스타일/홈 개선 커밋 2건으로 기능 단위 완료가 명확해져 리뷰/배포 추적성이 좋아졌다.
- Obsidian 볼트는 push blocker 제거 후 백업 커밋 흐름을 복구해 기록 자산의 원격 동기화 안정성을 확보했다.

### 🧾 이력서 후보 문장
- 복합 스크롤 구조(Table 9분할)에서 리사이즈·고정행 동기화 버그를 기준 시나리오/테스트 기반으로 수정해 `fix(react): [#82]` 단위로 회귀 리스크를 낮췄다.
- Alert Log 모듈을 상태 슬라이스부터 Split View UI까지 일괄 구현해 대시보드 탐색성 및 운영자 분석 효율을 개선했다.

### 📓 기록 활동
- 새로 작성/수정한 노트: [[Daily Notes/2026-02-11]], [[README]], [[Exem/06-Archive/ai-essence/TODO]], [[Exem/01-Projects/Table 컴포넌트/TODO]], [[Project/Resume with AI/개선 TODO]]
- 당일 Markdown 변경 파일 총 381개를 정리/백업했다.

### 💭 회고
재현 기준(스토리/테스트)을 먼저 고정하면 복잡한 UI 버그에서도 수정 범위를 통제할 수 있다. 기록 자동화는 커밋 단위와 연결될 때 회고와 이력서 소재로 바로 재사용 가능했다.
