# 🔧 Extjs 프로젝트 배포 프로세스 개선

## 도입 배경

1. 클라이언트 파일을 QA 나 고객사에 배포 할때 코드 압축과 난독화 하는 build 과정이 Gulp 프로젝트에 따로 존재합니다.
   - build 하기 위해 외부 프로젝트를 클론 받아서 이용해야 합니다
   - 클라이언트 파일을 gulp 프로젝트에 넣거나 symbolic link 를 걸어둬야 하므로, build 절차가 간결하지 않습니다.
   - 전체 파일을 build 해서 전달 하는 경우는 드물고 수정된 파일만 부분적으로 전달하는 경우가 많음

2. 릴리즈 할때 commit message 와 tag 명칭을 통일하기 위해 이전 commit 내역을 찾아서 복사 하는 과정이 번거롭습니다.
   - sql 프로젝트에도 같은 날짜로 tag 를 남겨둬야 나중에 찾기가 편한데 푸시를 안한다거나 하는 휴먼에러가 자주 발생합니다.

## 해결 방법

- 클라이언트 프로젝트 폴더에 gulp 프로젝트의 build 스크립트를 가져오고 각 프로젝트에서 build 를 바로 수행할 수 있도록 합니다.

1. `entire` 와 `partial`, 두 빌드 스크립트를 제공
   - `build entire`: 현재 working directory 에서 필요한 모든 파일을 압축
   - `build partial`: 수정되거나 추가된 파일(add 되야함) 을 대상으로 build 스크립트를 수행

2. `release` 스크립트를 추가해서 정해진 포멧대로 commit 메시지와 tag 명칭을 남김

## 사용 방법

- node 를 이용하므로 npm install, 혹은 pnpm install 을 로컬 프로젝트 폴더에서 선행

### 릴리즈할때
1. master 브랜치로 체크아웃 하여, versionControl.js 파일의 BuildNumber 를 원하는 값으로 변경
2. package.json 파일의 release tagging 스크립트를 실행하여 commit 과 tag 를 남기고, 원하는 경우 console 입력을 통해 origin 에 push
3. package.json 파일의 build entire 스크립트를 실행하여 전체 파일의 압축을 진행하고, .dist 폴더에 생성된 MAXGAUGE 폴더와, sql 프로젝트를 압축하여 전체 파일을 전달

### 부분 패치 파일 생성 할때
1. 수정된 파일을 working directory 에 넣어서 local changes 에서 목록을 확인
2. package.json 파일의 build partial 스크립트를 실행하여 부분 패치 파일을 생성하고, .dist 폴더에 생성된 파일을 전달

---
*ClickUp 원본: https://app.clickup.com/25540965/docs/rbeb5-443818/rbeb5-1738698*
